export NODE_VERSION=24

# nvm
if [ -s "$NVM_DIR/nvm.sh" ]; then
  . "$NVM_DIR/nvm.sh"
  nvm use "$NODE_VERSION" --silent || nvm install "$NODE_VERSION"
fi

# corepack (pnpm)
corepack enable 2>/dev/null
